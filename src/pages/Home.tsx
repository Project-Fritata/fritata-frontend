import { Text, Heading, VStack } from "@chakra-ui/react";

const Home = () => {
    return (
        <VStack>
            <Heading>FRItata</Heading>
            <Text>Social media network for FRI students</Text>
        </VStack>
    );
};

export default Home;
